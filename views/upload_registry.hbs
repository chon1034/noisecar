<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>上傳車籍資料</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f5f5f5;
    }
    .container {
      text-align: center;
      background-color: #fff;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      width: 400px;
    }
    h1 {
      margin-bottom: 30px;
    }
    .form-group {
      margin-bottom: 20px;
    }
    label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
    }
    input[type="file"],
    input[type="date"] {
      width: 100%;
      padding: 5px;
      font-size: 16px;
      margin-bottom: 10px;
    }
    button {
      display: block;
      width: 100%;
      padding: 10px;
      font-size: 16px;
      background-color: #6a5acd;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #483d8b;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>上傳車籍資料</h1>

    <!-- 上傳機車車籍 -->
    <form action="/upload-bike-registry" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="bikeQueryDate">查詢日期</label>
        <input type="date" id="bikeQueryDate" name="queryDate" max="9999-12-31" required>
        <label for="bikeFile">上傳機車車籍</label>
        <input type="file" id="bikeFile" name="bikeFile" accept=".csv" required>
      </div>
      <button type="submit">上傳機車車籍</button>
    </form>

    <hr>

    <!-- 上傳汽車車籍 -->
    <form action="/upload-car-registry" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="carQueryDate">查詢日期</label>
        <input type="date" id="carQueryDate" name="queryDate" max="9999-12-31" required>
        <label for="carFile">上傳汽車車籍</label>
        <input type="file" id="carFile" name="carFile" accept=".csv" required>
      </div>
      <button type="submit">上傳汽車車籍</button>
    </form>
  </div>
</body>
</html>